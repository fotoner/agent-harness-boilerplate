# Mistake Index

본 디렉토리의 모든 회고에서 식별된 실수의 1줄 요약 + 패턴 태그 + 누적 카운터.
`session-retrospective` 스킬은 회고 시작 시 이 파일을 참조해 반복 패턴을 식별하고 분류를 격상한다.

## 패턴 태그별 누적

| 패턴 태그 | 누적 횟수 | 마지막 발생 | 활성 규칙 |
|----------|----------|------------|----------|

(아직 누적된 패턴 없음. 회고가 쌓이면 행이 추가된다.)

## 회고 목록

(아직 회고 기록 없음. 첫 `session-retrospective` 실행 시 채워진다.)
