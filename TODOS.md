# TODOS

## Skills

- [ ] **init-project: `git add -A` → 특정 파일 add로 변경**
  **Priority:** P2
  init-project/SKILL.md:128-131에서 `git add -A`를 사용 중. CLAUDE.md의 커밋 규칙("특정 파일만 add")에 위반.
  `git add -A` 대신 init-project가 변경한 파일만 명시적으로 add하도록 수정.
  **Depends on:** 없음

## Completed
